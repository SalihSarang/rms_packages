import 'dart:ui';

/// A collection of primary colors used in the RMS application.
class PrimaryColors {
  /// The default primary color.
  static const Color defaultColor = Color(0xFF0066FF);

  /// The hover color.
  static const Color hoverColor = Color(0xFF0052CC);

  /// The light color.
  static const Color lightColor = Color(0xFFE6F0FF);

  /// The brand green color (Cashier Portal).
  static const Color brandGreen = Color(0xFF34D368);

  /// The active button color.
  static const Color buttonActive = Color(0xFF0066FF);
}
