import 'dart:ui';

/// A collection of status colors used in the RMS application.
class StatusColors {
  /// The pending color.
  static const Color pending = Color(0xFFFFC107);

  /// Pending background (Dark theme).
  static const Color pendingBg = Color(0xFF4A3515);

  /// Pending text (Dark theme).
  static const Color pendingText = Color(0xFFDCA835);

  /// The preparing color.
  static const Color preparing = Color(0xFF2196F3);

  /// Preparing background (Dark theme).
  static const Color preparingBg = Color(0xFF1D3254);

  /// Preparing text (Dark theme).
  static const Color preparingText = Color(0xFF5396FA);

  /// The ready color.
  static const Color ready = Color(0xFF4CAF50);

  /// The requests color.
  static const Color requests = Color(0xFF9C27B0);

  /// The paid color.
  static const Color paid = Color(0xFF9E9E9E);

  /// Completed/Paid background (Dark theme).
  static const Color completedBg = Color(0xFF3A3A3A);

  /// Completed/Paid text (Dark theme).
  static const Color completedText = Color(0xFFA0A0A0);

  /// The cancelled color.
  static const Color cancelled = Color(0xFFF44336);

  /// The purple light color.
  static const Color purpleLight = Color(0xFFB388FF);
}
