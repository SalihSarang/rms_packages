import 'dart:ui';

/// A collection of text colors used in the RMS application.
class TextColors {
  /// The primary text color.
  static const Color primary = Color(0xFFFFFFFF); // Updated for dark theme

  /// The secondary text color.
  static const Color secondary = Color(0xFFA0A0A0);

  /// The inverse text color.
  static const Color inverse = Color(0xFF0D1117);

  /// Muted text color for subtitles and hits.
  static const Color muted = Color(0xFF888888);
}
